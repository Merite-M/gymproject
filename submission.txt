I have fully resolved GYM-9 and addressed all review feedback.

Accomplished:
- Created the database migration (`20260812000000_marketing_automation.sql`) establishing the marketing and analytics tables with initial RLS.
- Addressed Review: Created `20260812000001_marketing_fixes.sql` adding a `UNIQUE(tenant_id, profile_id, snapshot_date)` constraint, dropping the permissive RLS policies, and implementing strict role-based RLS (`staff`/`admin`) using a `SECURITY DEFINER` function to avoid infinite recursion.
- Built the Node.js backend logic (`src/backend/marketing.js`) for the predictive churn trigger.
- Addressed Review: Added JWT authentication to the backend endpoint. Added member fetch pagination (bypassing the 1000 row limit). Added idempotency checks for inserting into `analytics_snapshots` (via `upsert`) and `member_workflow_state` (via pre-query check).
- Developed the Next.js staff marketing dashboard (`/staff/marketing/page.tsx`).
- Addressed Review: Removed hardcoded Supabase URLs and dummy keys. Filtered the snapshots query by the authenticated user's `tenant_id` and explicitly only fetching rows where `churn_risk_score >= 80`. Added a client-side de-duplication pass for the UI display.
- Passed frontend and backend compilation/linting successfully.
- Fixed Render deployment issue: bound Express to 0.0.0.0 to fix port scan timeout.
