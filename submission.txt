The sub-task GYM-18 for geofenced QR Code Validation has been implemented within the Always-On IoT Server backend endpoint \`/api/checkin\`.

Changes included:
1. Adding \`latitude\`, \`longitude\`, and \`geofence_radius_meters\` to the \`tenants\` Supabase table.
2. Expanding the \`checkins_status_check\` constraint on \`check_ins\` to permit a new \`denied_geofence\` state.
3. Updating \`src/backend/index.js\` \`/api/checkin\` endpoint to calculate Haversine distance if \`access_method === 'qr_code'\` and validating if coordinates are outside the required radius.
4. Marking the task as Completed in Linear.
